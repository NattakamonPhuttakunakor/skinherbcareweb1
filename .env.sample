# Sample environment variables (DO NOT commit real secrets)
# Copy to .env for local development (or set in Render/GCP/Azure)

# Node service
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://<username>:<password>@host:port/dbname
PYTHON_API_URL=http://127.0.0.1:5001/predict
PYTHON_API_KEY=fp_yolo_2026_secret_x93k

# Python service
# PYTHON_API_KEY must match the Node-side value above
